import { Button, Flex } from "@fabrix/core/src";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    type: {
      description: 'Define button type with several styles',
      options: ['primary', 'secondary', 'ghost', 'error', 'negative'],
      defaultValue: 'primary',
      control: { type: 'select' }
    },
    size: {
      options: ['xs', 'sm', 'md', 'lg', 'xl', 'auto'],
      control: { type: 'select' },
      defaultValue: 'md'
    },
    label: {
      control: { type: 'text' },
      defaultValue: 'Button'
    }
  }}
/>

# Button

Buttons are clickable elements that are used to trigger actions. They communicate calls to action to the user and allow users to interact with pages in a variety of ways. Button labels express what action will occur when the user interacts with it.
## Props

<ArgsTable of={Button} />

export const Template = args => <Button {...args}>{args.label}</Button>

## Examples

<Canvas>
  <Story name="Overview">
    <Flex direction="row" gap="compact">
      <Button>Hello from fabrix</Button>
      <Button type="secondary">Hello from fabrix</Button>
      <Button type="negative">Hello from fabrix</Button>
      <Button type="error">Hello from fabrix</Button>
      <Button type="ghost">Hello from fabrix</Button>
    </Flex>
  </Story>
  <Story name="Control">{Template.bind({})}</Story>
</Canvas>
